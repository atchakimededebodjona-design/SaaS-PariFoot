# PHASE 15.7.7 — COMMIT CONTRÔLÉ DU CORRECTIF REFERRAL PATHNAME

Généré : 2026-09-03 00:45:00 UTC

## 1-2. Vérifications git et périmètre

Diff de `frontend-design/api.js` confirmé strictement limité au correctif Phase 15.7.6 (`_slugCandidateFromPathname()` + extension de `captureReferralFromUrl()`). Périmètre attendu : exactement `frontend-design/api.js` + `frontend-design/test_referral_capture.js` (fait bien partie du périmètre versionné du projet).

## 3. Tests rejoués avant staging

| Suite | Résultat |
|---|---|
| `test_referral_capture.js` | **17/17** |
| Régression backend complète | **66/66** |
| `test_referral_promoter_platform.py` | 99/99 |
| `test_phase15_7_referral_attribution_repro.py` | 24/24 |

Tous verts avant tout staging.

## 4. Isolation IA

6 tables strictement identiques : `match=12459, match_stats=12459, model_predictions=3610, model_versions=15, team_ratings=568, prediction_log=9`.

## 5. Staging explicite

```
git add -- frontend-design/api.js frontend-design/test_referral_capture.js
```

`git diff --cached --name-only` → exactement les 2 fichiers attendus. Aucun `git add .`/`-A`.

## 6. Commit

**SHA `70970ee`** — `feat: fix referral capture from rewritten pathname`

## 7. Vérification post-commit

```
70970ee feat: fix referral capture from rewritten pathname
 frontend-design/api.js                   |  39 +++++++--
 frontend-design/test_referral_capture.js | 144 +++++++++++++++++++++++++++++++
 2 files changed, 176 insertions(+), 7 deletions(-)
```

`git show --name-only --format= HEAD` → exactement `frontend-design/api.js` et `frontend-design/test_referral_capture.js`. `git status --short` post-commit : plus aucun de ces 2 fichiers listé — seuls les fichiers étrangers pré-existants (`gates.py`) et hors périmètre de cette phase (`test_phase13.py`, `test_phase15_7_referral_attribution_repro.py`) restent, intacts.

## 8. Note pour le futur déploiement Hostinger

Le seul artefact runtime à déployer en production pour ce correctif est **`frontend-design/api.js`**. `test_referral_capture.js` est un test, reste dans le repository, **aucun upload prévu** sauf nécessité technique démontrée. Documenté pour référence — aucune action de déploiement entreprise cette phase.

## Contraintes respectées

Code non modifié avant commit · `.htaccess` NON · backend NON · Railway NON · DB NON · Chariow NON · prix NON · tables IA NON · `git add .`/`-A` NON · fichiers de rapport/temporaires/étrangers NON inclus · **push NON effectué**.

## Verdict

**`REFERRAL_FIX_COMMITTED_LOCALLY`**

Commit `70970ee` créé avec exactement les 2 fichiers attendus, vérifié par `git show --name-only`. Staging par chemin explicite uniquement. Tous les tests verts avant staging (17/17 + 66/66 + 99/99 + 24/24). 6 tables IA inchangées. Aucun push.

---

**PHASE 15.7.7 — COMMIT LOCAL DU CORRECTIF REFERRAL PATHNAME RÉUSSI. SHA 70970ee, EXACTEMENT 2 FICHIERS (frontend-design/api.js, frontend-design/test_referral_capture.js), VÉRIFIÉ PAR git show --name-only. STAGING PAR CHEMIN EXPLICITE UNIQUEMENT (JAMAIS git add . / -A). 17/17 + 66/66 + 99/99 + 24/24 TESTS VERTS AVANT STAGING. 6 TABLES IA STRICTEMENT INCHANGÉES. AUCUN FICHIER HORS PÉRIMÈTRE INCLUS (RAPPORTS, TEMPORAIRES, ÉTRANGERS). AUCUNE MODIFICATION .htaccess/BACKEND/RAILWAY/DB/CHARIOW/PRIX. AUCUN PUSH EFFECTUÉ CETTE PHASE. VERDICT : REFERRAL_FIX_COMMITTED_LOCALLY — EN ATTENTE D'AUTORISATION HUMAINE SÉPARÉE POUR LE PUSH ET LE DÉPLOIEMENT HOSTINGER.**
