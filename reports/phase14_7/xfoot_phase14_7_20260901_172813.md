# PHASE 14.7 — XFOOT PRODUCTION DEPLOYMENT & CONTROLLED SMOKE TEST V1

Généré : 2026-09-01 17:28:13 UTC · Release : **`ebb23ec`**

## 1. Executive Summary

**Découverte majeure, confirmée par preuve directe** : le push vers `origin/main` a eu lieu (par l'utilisateur, hors de cette conversation) **ET Railway a auto-déployé le backend** — les 11 routes Phase 14 sont désormais **réellement live** sur `api.xfoot.site` (openapi.json : 36 → 47 routes, exactement les 11 attendues). Le frontend Hostinger **n'est pas déployé** (pages Phase 14 toujours 404 — attendu, pas d'auto-déploiement). Interrogé sur la création de comptes de test réels en production, l'utilisateur a choisi **lecture seule uniquement**. Tous les tests non-destructifs ont été exécutés réellement contre la production ; les tests nécessitant des données réelles sont marqués `BLOCKED_PENDING_HUMAN_DECISION`, jamais inventés.

## 2. Git Verification

`CURRENT_HEAD = ebb23ec` ✅ · **`origin/main` (via `git ls-remote`, lecture seule) = `ebb23ecd33729aca5b78731f1a062b936a79c801` = ebb23ec** ✅

Le push a réellement eu lieu — fait vérifié, jamais supposé. Aucun nouveau commit créé cette phase, aucun reset/rebase/amend/force-push.

## 3. Deployment Access

`RAILWAY_ACCESS = UNAVAILABLE` · `HOSTINGER_ACCESS = UNAVAILABLE` (toujours aucun CLI/token depuis cet environnement). **Mais le déploiement backend a manifestement eu lieu** via l'auto-déploiement Railway (connecté à GitHub) — observation factuelle, aucune action de déploiement entreprise depuis cette conversation.

## 4. Environment

| Variable | Statut |
|---|---|
| **ADMIN_EMAILS** | **UNKNOWN** |
| DATABASE_URL | UNKNOWN (présumée configurée) |
| CORS/ALLOWED_ORIGINS | CONFIGURED (fonctionnellement) |
| BACKEND_URL | **CONFIRMED** (200 sur /health) |
| CHARIOW_* | UNKNOWN en production |

`ADMIN_EMAILS=UNKNOWN` jamais transformé en PASS.

## 5. Database

`DB_TARGET`/`DB_SCHEMA`/`CURRENT_MIGRATION` : **UNKNOWN** (aucun accès DB). `EXPECTED_MIGRATION = 5787c4b3b963`. `api/app.db` local **non touché** (confirmé §15). Aucune migration exécutée par cette conversation.

## 6. Migration

Revision `5787c4b3b963` (← `c4f8a1d75e93`), `upgrade()`/`downgrade()` présents, 0 référence DDL aux tables IA. **Non exécutée par cette conversation.** Indice indirect (pas une preuve) : les routes répondent sans erreur 500, cohérent avec des tables créées — documenté comme indice, jamais comme certitude.

## 7. Backend Deployment

**`BACKEND_STATUS = DEPLOYED`** — confirmé par preuve HTTP directe. `DEPLOYED_COMMIT` inféré avec très haute confiance = `ebb23ec` (correspondance exacte des 11 routes + `origin/main == ebb23ec`), mais **aucune preuve cryptographique directe** (pas d'endpoint de version). `HEALTH_STATUS = OK` (`GET /health` → 200, JSON valide). Aucune erreur startup visible.

## 8. Frontend Deployment

**`FRONTEND_STATUS = NOT_DEPLOYED`** — `promoter.html`/`admin-*.html` → 404 (×4). Mécanisme identifié : upload manuel Hostinger requis, jamais exécuté. Pas un bug.

## 9. HTTPS / Routing

`http://` → 301 → `https://` ✅ · `https://www.xfoot.site` → 200 ✅ · `https://api.xfoot.site/health` → 200 ✅. **11/11 routes attendues** (extraites du code réel, jamais inventées) présentes dans `openapi.json` de production.

## 10. Admin

**`ADMIN_ACCESS_VALIDATION = BLOCKED`** — `ADMIN_EMAILS` UNKNOWN, aucun identifiant admin réel disponible. Vérifié réellement : `GET /admin/*` sans token → 401 (×3).

## 11. Promoter

**`BLOCKED_PENDING_HUMAN_DECISION`** — décision utilisateur : lecture seule uniquement. Vérifié réellement : `GET /promoter/me*` sans token → 401 (×3). Format du lien confirmé par le code : `https://www.xfoot.site/{slug}`.

## 12. Referral

**`BLOCKED_PENDING_HUMAN_DECISION`** — routes présentes et confirmées existantes (`openapi.json`), non invoquées avec des données réelles. `CLICK ≠ REGISTRATION ≠ CHECKOUT ≠ PENDING ≠ SALE` reconfirmé par lecture de code (déjà prouvé par 135 assertions en isolé, Phase 14/14.1).

## 13. Commission

Invariant reconfirmé par lecture de code (désormais live en production, même contenu que `ebb23ec`) : `commission = floor(gross_paid × 4000bp / 10000)`. Exemple : 20 000 catalogue / 15 000 payé → **6 000**, jamais 8 000. Frontend ne peut jamais imposer la commission (confirmé par lecture de code). **Aucun test financier réel** (règle absolue).

## 14. Webhook

| Test | Méthode | Résultat |
|---|---|---|
| Signature invalide | POST `/billing/pulse` avec signature falsifiée | **401 — REJECT confirmé réellement** |
| Signature absente | POST `/billing/pulse` sans header | **401 — REJECT confirmé réellement** |

Sans risque : le code rejette avant tout accès DB (vérifié dans le commit déployé). Signature valide/replay/doublon : **non testables** sans le vrai secret Chariow (jamais accessible) — déjà prouvés en isolé (Phase 14).

## 15. Database Purity

Local dev DB : identique avant/après cette phase (12459/12459/3610/15/568/9). **Production : non vérifiable directement** (pas d'accès DB) — garantie architecturale confirmée (aucun module referral ne référence les tables IA), documentée honnêtement comme non mesurée empiriquement en production.

## 16. AI Isolation

Aucun entraînement, promotion, modification de prédiction, signal, Kill Switch, MODE_3/4, scheduler IA, ou appel Odds API — code déployé = commit déjà audité, 0 changement.

## 17. Production Health

Health 200 OK. Aucune erreur startup/DB/HTTP inattendue visible. Pas de fenêtre de monitoring prolongée (logs Railway inaccessibles) — vérification ponctuelle uniquement.

## 18. Production Version

**`PRODUCTION_VERSION`** : `UNKNOWN` au sens preuve directe / **`ebb23ec`** au sens inférence de très haute confiance (aucun endpoint de version exposé — jamais déduit simplement parce que le déploiement était attendu).

## 19. Smoke Test Matrix

| Test | Expected | Actual | Status |
|---|---|---|---|
| HTTPS | PASS | 200/301/200 | **PASS** |
| API health | PASS | 200 OK | **PASS** |
| Frontend | PASS | domaine sain, pages Phase 14 absentes | PARTIAL |
| Referral URL | PASS | routes présentes (non invoquées) | PASS (existence) |
| Promoter login | PASS | non observé | BLOCKED_PENDING_HUMAN_DECISION |
| Promoter isolation | PASS | non observé (prouvé isolé) | BLOCKED_PENDING_HUMAN_DECISION |
| Admin login | PASS | non observé | BLOCKED (ADMIN_EMAILS UNKNOWN) |
| Admin isolation | PASS | 401 ×3 confirmé | PASS (partiel) |
| Referral attribution | PASS | non observé | BLOCKED_PENDING_HUMAN_DECISION |
| Pending payment | 0 commission | non observable (prouvé isolé) | NOT_OBSERVED |
| Invalid webhook | REJECT | 401 confirmé réellement | **PASS** |
| Duplicate webhook | IDEMPOTENT | non testable (prouvé isolé) | NOT_OBSERVED |
| AI DB purity | UNCHANGED | local confirmé, prod non vérifiable | PASS (local) / UNVERIFIABLE (prod) |

## 20. Rollback

`CURRENT_RELEASE = ebb23ec` · `PREVIOUS_RELEASE = 2160b4f`. Application : historique Railway · Git : `git revert` (doc only) · DB : `alembic downgrade -1` (doc only). **Rien exécuté.**

## 21. Payment Gate

**`PAYMENT_REAL_TEST = PENDING_HUMAN_AUTHORIZATION`** · `PAYMENT_ENVIRONMENT = UNKNOWN` · 0 appel Chariow. → **`PAYMENT_VERIFICATION = BLOCKED_PENDING_HUMAN_AUTHORIZATION`**, gate séparé du succès du déploiement.

## 22. Limitations

`PRODUCTION_VERSION` sans preuve cryptographique · frontend non déployé · `ADMIN_EMAILS` UNKNOWN · tests Promoteur/Admin authentifiés et attribution complète non exécutés (décision utilisateur) · pureté DB production non mesurable directement · pas de fenêtre de monitoring prolongée · `PAYMENT_ENVIRONMENT` UNKNOWN · idempotence/refund webhook non re-testables en réel.

## 23. Final Verdict

**`DEPLOYED_WITH_LIMITATIONS`**

Backend réellement déployé et vérifié par preuve HTTP directe (11/11 routes live, health OK, 401 corrects, sécurité webhook confirmée réellement). Frontend non déployé. Tests à données réelles explicitement bloqués sur décision utilisateur, jamais contournés ni simulés. Aucune modification IA, aucun paiement réel, aucune action irréversible. Jamais `PRODUCTION_READY`, `PAYMENT_VERIFIED`, ni `PRODUCTION_BETTING_ACTIVATED`.

---

## Human Decision Log

**Question posée** : comment procéder pour le smoke test d'attribution nécessitant des comptes de test réels en production ?
**Réponse** : *Rester en lecture seule uniquement* (recommandé par défaut).
**Respectée** : oui, intégralement.

## Absolute Rules Compliance

Moteur IA non modifié · aucun modèle promu/entraîné · aucun signal de pari · aucun appel Odds API · aucun paiement réel · Kill Switch non désactivé · MODE_3/4 non activés · ADMIN_EMAILS non modifié · authentification/HMAC non contournés · aucune donnée/compte supprimé · aucun downgrade DB · aucun git push/reset/rebase/amend/force-push cette phase.

---

**PHASE 14.7 — XFOOT PRODUCTION DEPLOYMENT & CONTROLLED SMOKE TEST V1 TERMINÉE. LE RELEASE ebb23ec A ÉTÉ DÉPLOYÉ ET VÉRIFIÉ, OU LES LIMITATIONS D'ACCÈS/ENVIRONNEMENT ONT ÉTÉ DOCUMENTÉES HONNÊTEMENT. ROUTING, HTTPS, ADMIN, PROMOTEUR, ATTRIBUTION REFERRAL, ISOLATION, COMMISSION DE 40% ET SÉCURITÉ WEBHOOK ÉVALUÉS OU LIMITATIONS DOCUMENTÉES. AUCUN PAIEMENT RÉEL EFFECTUÉ SANS AUTORISATION HUMAINE SÉPARÉE. AUCUNE MODIFICATION DU MOTEUR IA. AUCUN SIGNAL DE PARI PRODUCTION GÉNÉRÉ.**
