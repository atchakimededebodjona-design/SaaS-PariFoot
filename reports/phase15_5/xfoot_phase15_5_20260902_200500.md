# PHASE 15.5 — XFOOT POST-DÉPLOIEMENT : OFFRES V15 + NON-RÉGRESSION DES ABONNEMENTS EXISTANTS

Généré : 2026-09-02 20:05:00 UTC · SHA : `eeb01ec`

## 1-2. Git / origin

`HEAD` = `eeb01ec`, `origin/main` = `eeb01ec` (confirmé). Scope du commit re-vérifié : exactement les 6 fichiers attendus, limité au pricing. Aucun commit, aucun push effectué cette phase.

## 3. Railway

`GET /health` → `200 OK`. **Limite honnête** : aucun endpoint de version n'existe — le SHA exact du processus en cours ne peut pas être prouvé positivement par HTTP.

## 4. Hostinger — ✅ DÉPLOYÉ

Entre la Phase 15.4 (19:31 UTC, anciennes offres) et cette phase (19:45 UTC), l'upload manuel a été effectué.

| Fichier | Preuve |
|---|---|
| `billing.html` | Contenu production **identique byte-for-byte** au blob `git show eeb01ec:frontend-design/billing.html` |
| `vip.html` | Idem, identique byte-for-byte |

## 5-7. Prix / anciennes offres / badge

Prix visibles en production : **1000 FCFA / 1500 FCFA / 28000 FCFA** sur les deux pages. Aucune occurrence de « 10000 FCFA » ni « 1000 FCFA/mois ». Badge « Économisez 2 mois » absent — confirmé.

## 8. Architecture checkout

`billing.html` appelle `apiFetch('/billing/checkout', ...)` — **aucun lien statique `mychariow.co`** trouvé dans `billing.html` ni `vip.html` (page d'aperçu, redirige vers `billing.html`). Le backend conserve intact :
- `user_id` dans `custom_metadata` (router.py:257)
- contrôle du prix côté serveur (`PRODUCT_IDS[body.plan]`, jamais un montant client)
- sécurité webhook (`hmac.compare_digest`)
- idempotence (`ProcessedPulseDelivery` + contrainte UNIQUE)
- commission créée UNIQUEMENT après confirmation de paiement

## 9. Product IDs

| Plan | Product ID |
|---|---|
| `biweekly` | `prd_1gje3jzz` |
| `monthly` | `prd_sgvapilx` |
| `yearly` | `prd_f90jpbh3` |

**Note d'architecture** : les clés `monthly`/`yearly` sont réutilisées (pas de 4ᵉ clé) — les variables d'environnement portent désormais les NOUVEAUX Product ID pour les NOUVEAUX achats. Ceci n'affecte JAMAIS un `ProviderSubscription` déjà écrit (ses champs viennent du webhook Chariow au moment de l'achat passé, jamais recalculés depuis `PRODUCT_IDS`).

## 10. Non-régression des abonnements existants — analyse de code

| Recherche | Résultat |
|---|---|
| Logique « ancien plan → nouveau plan » | **AUCUNE TROUVÉE** |
| Logique « ancien prix → nouveau prix » | **AUCUNE TROUVÉE** |
| Expiration automatique liée au catalogue | **AUCUNE TROUVÉE** |
| Recalcul rétroactif de commission | **AUCUNE TROUVÉE** — `ReferralCommission` n'a aucun chemin UPDATE sur les montants, seulement sur `status` (ACCRUED→REVERSED) lors d'un remboursement réel |

Fichiers inspectés : `billing/router.py`, `billing/backfill.py` (migration one-shot déjà appliquée, idempotente par skip), `billing/entitlement_service.py`, `referral/commission_service.py`, `referral/money.py`.

**Point d'attention mineur (non bloquant, sans impact sur l'existant)** : `PLAN_LIST_PRICES` (repli de secours si le webhook Chariow ne porte aucun montant) ne couvre que `monthly`/`yearly`, pas `biweekly` — concerne uniquement un scénario futur, aucun abonnement existant affecté.

## 11. Cas concrets A-E

**Méthode** : analyse statique uniquement — aucun accès DB production, aucun compte de test avec historique d'ancien plan fourni cette phase.

| Cas | Statut |
|---|---|
| A. Ancien utilisateur 1000/mois conserve son historique | `NOT_EMPIRICALLY_VERIFIED` (garanti par absence structurelle de logique de modification) |
| B. Ancien utilisateur 10000/an conserve son historique | `NOT_EMPIRICALLY_VERIFIED` (idem) |
| C. Pas de transformation automatique vers un nouveau plan | **Confirmé par analyse statique** — aucune route ne réécrit `plan` hors achat/activation explicite |
| D. Ancien abonnement expiré non supprimé | **Confirmé par analyse statique** — aucune route DELETE sur `ProviderSubscription` |
| E. Ancienne commission non recalculée | **Confirmé par analyse statique** |

**Verdict d'étape : `HISTORICAL_ACTIVE_SUBSCRIPTION_TEST_BLOCKED`** — aucune donnée fabriquée, blocage déclaré honnêtement.

## 12. Renouvellements Chariow

Aucun appel Xfoot ne modifie/supprime/désactive un produit Chariow (seuls appels d'écriture : `POST /checkout`, `POST /licenses/{key}/activate`, sans rapport avec la gestion produit). Le comportement réel des renouvellements Chariow pour les anciens produits est **NON VÉRIFIABLE** depuis le code Xfoot (système externe) — documenté honnêtement, jamais supposé.

## 13. Promoteur / commission

Formule inchangée : `commission = floor(gross_paid × rate_bp / 10000)`, jamais basée sur un prix catalogue ni fournie par le client. Vérifications théoriques (testées) : 1000→400, 1500→600, 28000→11200 FCFA. Aucune transaction créée.

## 14. Tests

**65/65 fichiers, 0 échec réel** (exit code vérifié). Suites ciblées re-vérifiées individuellement : `test_referral_promoter_platform.py` (99/99), `test_chariow_billing.py` (30/30), `test_phase15_1_new_offers.py` (17/17), `test_phase14_1_production_integration.py` (36/36), `test_auth.py` (8/8), `test_entitlement.py` (8/8), `test_provider_subscription_backfill.py` (4/4).

## 15. 6 tables IA

Strictement identiques avant/après : `match=12459, match_stats=12459, model_predictions=3610, model_versions=15, team_ratings=568, prediction_log=9`. Aucun fichier `app/ai/*` dans le commit `eeb01ec`.

## 16. Limites de vérification

- Pas d'endpoint de version backend.
- Aucun accès DB production — non-régression des abonnements garantie par le CODE, pas observée empiriquement.
- Aucun compte de test avec historique d'ancien plan fourni cette phase.
- Renouvellements Chariow externes non observables.
- `PLAN_LIST_PRICES` sans entrée `biweekly` (gap mineur, futur uniquement).

## Verdict Final

**`PRICING_DEPLOYED_HISTORICAL_SUBSCRIPTION_CHECK_BLOCKED`**

Le nouveau pricing est confirmé déployé (preuve byte-for-byte). L'architecture checkout/referral/commission est intacte. L'analyse statique ne révèle aucune logique de régression — mais faute d'accès DB production ou de compte de test historique, le point critique (Cas A/B) reste marqué `BLOCKED` par honnêteté méthodologique, non `PASS`.

---

## Résumé Court

- frontend production : **PASS**
- nouvelles offres : **PASS**
- anciennes offres retirées de l'interface : **PASS**
- abonnements existants non impactés : **NOT_VERIFIABLE** (garantie forte par code, pas d'observation empirique)
- renouvellements Chariow : **NOT_VERIFIABLE** (système externe)
- promoteur : **PASS**
- tests : **65/65**
- IA : **UNCHANGED**
- verdict final : **PRICING_DEPLOYED_HISTORICAL_SUBSCRIPTION_CHECK_BLOCKED**

---

**PHASE 15.5 — POST-DÉPLOIEMENT OFFRES V15 + NON-RÉGRESSION : PRICING CONFIRMÉ DÉPLOYÉ EN PRODUCTION (billing.html ET vip.html IDENTIQUES BYTE-FOR-BYTE AU COMMIT eeb01ec, ANCIENNES OFFRES/BADGE ABSENTS). ARCHITECTURE CHECKOUT/REFERRAL/COMMISSION VÉRIFIÉE INTACTE. AUCUNE LOGIQUE DE MIGRATION, RECALCUL RÉTROACTIF OU EXPIRATION AUTOMATIQUE TROUVÉE DANS LE CODE — GARANTIE STRUCTURELLE FORTE MAIS NON CONFIRMÉE EMPIRIQUEMENT SUR UN COMPTE RÉEL HISTORIQUE (AUCUN ACCÈS DB PRODUCTION, AUCUN COMPTE DE TEST FOURNI CETTE PHASE). RENOUVELLEMENTS CHARIOW EXTERNES NON VÉRIFIABLES DEPUIS LE CODE XFOOT. 65/65 TESTS VERTS (EXIT CODE RÉEL), 6 TABLES IA STRICTEMENT INCHANGÉES. AUCUN PAIEMENT RÉEL, AUCUNE ÉCRITURE CHARIOW, AUCUN COMPTE CRÉÉ, AUCUN ABONNEMENT MODIFIÉ, AUCUN HISTORIQUE SUPPRIMÉ, AUCUN PRODUIT CHARIOW ANCIEN TOUCHÉ, AUCUN COMMIT, AUCUN PUSH. VERDICT : PRICING_DEPLOYED_HISTORICAL_SUBSCRIPTION_CHECK_BLOCKED — EN ATTENTE D'UN MOYEN DE VÉRIFICATION EMPIRIQUE (COMPTE DE TEST HISTORIQUE OU ACCÈS LECTURE ADMIN) POUR LEVER CE BLOCAGE.**
