# PHASE 15.6 — ÉTAPE 1 : PRÉ-CHECKS AVANT PAIEMENT

Généré : 2026-09-02 20:17:00 UTC

## Accès disponibles cette session

Aucun identifiant/token pour `payment@test.com`, `promoter-2`, ou un compte admin de test. Aucun accès direct à la base Postgres de production. Aucun outil de navigateur. Seuls des appels à des endpoints **publics et sans écriture** sont possibles.

## Check 1 — `payment@test.com`

**NOT_VERIFIABLE.** Aucun endpoint public ne permet de vérifier l'existence d'un compte par email sans risque d'effet de bord : `POST /auth/register` créerait le compte s'il n'existe pas encore (strictement interdit), `POST /auth/login` exige un mot de passe (jamais demandé en chat), et il n'existe aucun endpoint « vérifier un email »/« mot de passe oublié » dans le code (`api/app/auth/router.py` ne contient que `/register`, `/login`, `/me`).

## Check 2 — `promoter-2`

| | Résultat |
|---|---|
| Existe | **PASS** (vérifié) |
| Status ACTIVE | **PASS** (vérifié) |
| user_id = 206 | **NOT_VERIFIABLE** |

**Méthode** : `POST https://api.xfoot.site/referral/resolve/promoter-2` avec `body={}` — **sans** `visitor_id`, ce qui garantit **zéro écriture en base** (le code ajoute une ligne `ReferralVisit` uniquement si `visitor_id` est fourni, voir `app/referral/router.py:65-81`).

**Résultat** : `200 OK`, `{"valid": true}`.

**Interprétation** : par construction du code (commentaire explicite §32), `valid: true` signifie exactement « le slug existe ET son statut est ACTIVE » — un slug inexistant et un promoteur suspendu renvoient tous deux `valid: false`, volontairement indifférenciés pour ne jamais exposer d'information administrative à un appelant non authentifié. Ceci confirme donc existence + ACTIVE, mais **ne peut pas** confirmer `user_id=206` (donnée volontairement non exposée par cet endpoint public, par design de confidentialité).

## Check 3 — Séparation acheteur/promoteur

**NOT_VERIFIABLE** sur les 3 sous-points (emails, user_id, rôle) — nécessite un accès authentifié (admin dashboard ou token) qui n'est pas disponible cette session.

## Check 4 — Activité commerciale préalable

**NOT_VERIFIABLE.** Ventes/commissions/attribution existantes pour `payment@test.com` : aucune preuve read-only obtenue. (Note : il a été indiqué oralement dans la conversation qu'il s'agit d'un compte neuf sans paiement antérieur — information retenue mais **non vérifiée techniquement**, conformément à la règle de ne jamais inférer.)

## Check 5 — Vérification production

Site et backend accessibles (`200 OK` sur `/` et `/health`, déjà confirmé en Phases 15.4/15.5). Aucun contournement d'authentification, aucun faux token, aucune permission modifiée.

## Aucune modification effectuée

DB : NON · Chariow : NON · Compte : NON · Paiement : NON · Code : NON.

## Verdict

**`NOT_VERIFIABLE`**

1 des 3 conditions requises est confirmée (promoter-2 existe et est ACTIVE, prouvé sans aucune écriture). Les 2 autres (existence de `payment@test.com`, séparation stricte acheteur/promoteur par `user_id`) ne peuvent pas être établies avec les moyens disponibles cette session. Aucune condition n'a activement échoué — c'est un blocage structurel d'accès, pas un problème détecté sur les comptes eux-mêmes. Conformément à la règle explicite de cette étape, ceci est donc déclaré `NOT_VERIFIABLE` plutôt qu'inféré comme PASS ou comme un `STOP_x` spécifique.

---

## Sortie finale

```
ÉTAPE 1 — PRÉ-CHECKS

payment@test.com existe : NOT_VERIFIABLE
user_id acheteur : NOT_VERIFIABLE
acheteur promoteur : NOT_VERIFIABLE

promoter-2 existe : PASS
promoter-2 user_id : NOT_VERIFIABLE (endpoint public ne l'expose pas)
promoter-2 status : ACTIVE (PASS)

acheteur ≠ promoteur : NOT_VERIFIABLE

vente payée existante avant test : NOT_VERIFIABLE
commission existante avant test : NOT_VERIFIABLE

Modification effectuée : NON

VERDICT : NOT_VERIFIABLE
```

**Action requise** : relais humain nécessaire (admin dashboard, ou token en lecture seule) pour confirmer (a) l'existence et le `user_id` de `payment@test.com`, (b) que ce `user_id` diffère de celui de `promoter-2` et qu'il n'a pas le rôle promoteur, (c) l'absence de vente/commission préexistante. Je m'arrête ici, comme demandé — pas de passage automatique à l'Étape 2.
