# PHASE 15.6 — ÉTAPE 2 : ATTRIBUTION VIA LE LIEN PROMOTEUR

Généré : 2026-09-02 20:34:00 UTC

## 1-3. Acteurs et lien

| | |
|---|---|
| Acheteur | `payment@test.com`, user_id=209 (relayé, non re-vérifié techniquement cette étape) |
| Promoteur | `promoter-2`, user_id=206, ACTIVE (re-confirmé techniquement en Étape 1) |
| URL referral | `https://www.xfoot.site/promoter-2` |

## 4-5. Attribution — NOT_OBSERVABLE_BY_ASSISTANT

**Ce que j'ai pu vérifier (lecture seule, sans session)** :
- `curl -i -L https://www.xfoot.site/promoter-2` → `HTTP 200`, contenu = `login.html` (« xFoot - Connexion »), **aucune redirection HTTP** — confirme que la réécriture `.htaccess` interne fonctionne comme documenté.
- Ceci prouve que le **mécanisme d'accès** à l'URL fonctionne — **pas** que l'attribution serveur a été créée pour ce compte, ce qui nécessite une connexion réelle + exécution du JS client (`captureReferralFromUrl`/`attributeReferralIfPresent`) + `POST /referral/attribute` avec un token valide.

**Ce que je ne peux pas faire** : je n'ai ni outil de navigateur, ni identifiants pour `payment@test.com` cette session — impossible d'exécuter moi-même la connexion, la navigation JS, ou l'appel authentifié qui établirait réellement l'attribution.

## 6. État avant paiement

Abonnement / vente / commission de `payment@test.com` : **NOT_VERIFIABLE** par cet assistant (aucun accès aux endpoints authentifiés). L'énoncé indiquait « 0 ventes / 0 FCFA » — information relayée, retenue mais non re-vérifiée techniquement ici.

## 7. Checkout

**Non tenté** — nécessite une session authentifiée que je n'ai pas. Product ID/montant : non observés. **Paiement effectué : NON.**

## 8. Modifications

Compte : NON · DB : NON · Chariow : NON · Abonnement : NON · Commission : NON.

## 9. Limitations

- Aucun outil de navigateur disponible.
- Aucun token pour `payment@test.com`/`promoter-2`/admin cette session.
- Seule la résolution HTTP de l'URL et la validité publique du slug ont pu être vérifiées techniquement — l'attribution effective pour CE compte reste non observée par cet assistant.
- Un relais humain (parcours réel dans un navigateur + lecture des dashboards) est nécessaire pour compléter cette étape.

## Verdict

**`ATTRIBUTION_PRE_PAYMENT_NOT_OBSERVABLE`**

Non pas parce que le mécanisme serait cassé — la résolution d'URL fonctionne correctement, signal positif indirect — mais parce que je n'ai techniquement aucun moyen d'exécuter les actions requises (connexion, navigation JS, appel authentifié) sans navigateur ni identifiants.

---

## Sortie finale

```
ÉTAPE 2 — ATTRIBUTION REFERRAL

Acheteur : payment@test.com
USER ID : 209

Promoteur : promoter-2
USER ID : 206 (relayé) / ACTIVE confirmé techniquement
Statut promoteur : ACTIVE

Lien referral : https://www.xfoot.site/promoter-2
Résolution URL : PASS (200, sert login.html, .htaccess fonctionne)

Attribution referral : NOT_OBSERVABLE (par cet assistant — nécessite navigateur/token)

Preuve : résolution HTTP de l'URL confirmée ; établissement effectif de
l'attribution serveur non observable sans session authentifiée.

Vente payée avant paiement : NOT_VERIFIABLE (relayé : 0)
Commission avant paiement : NOT_VERIFIABLE (relayé : 0 FCFA)

Checkout atteint : NON
Paiement effectué : NON

Modification effectuée : NON

VERDICT : ATTRIBUTION_PRE_PAYMENT_NOT_OBSERVABLE
```

**Important** : je m'arrête ici, comme demandé. Pas de checkout, pas de paiement. Pour avancer, il faudrait soit que tu exécutes toi-même le parcours (connexion `payment@test.com` → clic sur le lien → observation admin dashboard) et me relaies le résultat, soit me fournir un token en lecture seule pour ce compte.
