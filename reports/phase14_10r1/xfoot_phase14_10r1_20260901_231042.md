# PHASE 14.10R.1 — XFOOT ASSISTED TEST ACCOUNT PROVISIONING

Généré : 2026-09-01 23:10:42 UTC · Release : **`ebb23ec`**

**STOP-BEFORE-PRODUCTION-WRITE — phase préparatoire uniquement. Aucune écriture production effectuée.**

## 1. Prérequis (vérifiés en lecture seule)

| Vérification | Résultat |
|---|---|
| Frontend production | `https://www.xfoot.site/` → 200 |
| `login.html` | 200 |
| `admin-promoters.html` | 200 |
| API production | `https://api.xfoot.site/health` → 200 |
| Endpoint inscription | `OPTIONS /auth/register` → 405 (route confirmée existante) |
| Endpoint authentification | `OPTIONS /auth/login` → 405 (route confirmée existante) |
| Route admin protégée | `GET /admin/promoters` sans auth → 401 |
| Hotfix `api.js` toujours actif | Confirmé — `applyRoleBasedNav`/`captureReferralFromUrl` toujours présents, stable depuis Phase 14.9R.2 |

Aucune écriture effectuée.

## 2. État Git

`HEAD = ebb23ecd33729aca5b78731f1a062b936a79c801` = `ebb23ec` ✅. Seuls les 2 fichiers Phase 13 pré-existants restent modifiés (étrangers). Aucune modification par cette phase.

## 3. Comptes de Test

**Propositions de la Phase 14.10R, jamais confirmées par l'humain à ce jour :**
- Admin : `xfoot.qa.admin+phase14@gmail.com`
- Promoteur : `xfoot.qa.promoter+phase14@gmail.com`

**Avant toute création réelle**, l'humain doit confirmer ces adresses ou en fournir deux autres dédiées aux tests. Aucune adresse réelle inventée. Mots de passe jamais demandés ni affichés.

## 4. Procédure Admin (documentée, non exécutée)

1. Créer le compte utilisateur Admin via `login.html` (inscription standard, aucun champ de rôle).
2. Sur Railway (service SaaS-PariFoot) → Variables d'environnement → `ADMIN_EMAILS`.
3. **ÉTENDRE** (jamais remplacer) la liste CSV existante avec l'email de test.
4. Effet immédiat, aucun redéploiement.
5. Se connecter avec ce compte.
6. Vérifier : `GET /admin/promoters` → 200 attendu.

**Risque critique** : si `ADMIN_EMAILS` est **redéfini** au lieu d'**étendu**, tout admin déjà autorisé perdrait immédiatement son accès. Toujours lire la valeur actuelle avant modification.

## 5. Procédure Promoteur (documentée, non exécutée)

1. Créer le compte utilisateur Promoteur via `login.html`.
2. Se connecter avec le compte Admin (déjà provisionné, §4).
3. Ouvrir `admin-promoters.html`.
4. Récupérer le `user_id` du compte Promoteur de test (via `admin-subscribers.html` ou `GET /admin/subscribers`).
5. Utiliser « Créer un promoteur ».
6. Vérifier le slug généré.
7. Vérifier `commission_rate_percent = 40.0`.
8. Vérifier le statut initial `ACTIVE`.
9. Se connecter avec le compte Promoteur, vérifier `promoter.html`.

## 6. Sécurité

| Protection | Vérifiée |
|---|---|
| Utilisateur normal → Admin | **PASS** — aucun champ de rôle exposé |
| Utilisateur normal → créer un Promoteur | **PASS** — `require_admin` |
| `promoter_id` imposé par le client | **PASS** — dérivé du JWT uniquement |
| Routes admin protégées | **PASS** — 401 sans token, reconfirmé réellement |
| Routes promoteur protégées | **PASS** — déjà reconfirmé (Phase 14.9/14.10) |
| Désactivation réversible | **PASS** — `POST /admin/promoters/{id}/status` |
| Aucune suppression destructive | **PASS** — aucun endpoint DELETE, choix intentionnel |

**Verdict sécurité : PASS.**

## 7. Plan de Nettoyage

**Mécanisme disponible : désactivation uniquement** — `POST /admin/promoters/{id}/status` → `INACTIVE`, après les tests. Historique intact et consultable. Compte Admin de test : retirer son email de `ADMIN_EMAILS`. Aucune API de suppression n'a été inventée ; aucune donnée financière n'est jamais supprimée.

## 8. Plan Phase 14.11

**ADMIN** : login → dashboard promoteurs → abonnés → gains.
**PROMOTEUR** : login → `/promoter/me` → dashboard → referral link → conversions/ventes/revenus/commissions.
**SÉCURITÉ** : non-authentifié → 401/403 · Promoteur→Admin → 401/403 · `promoter_id` arbitraire → refus · isolation A/B (hors périmètre, nécessiterait un second compte).
**PAIEMENT** : aucun, aucun appel Chariow.

**Prérequis** : confirmation humaine que les 2 comptes existent et que `ADMIN_EMAILS` a été étendu.

## 9. Vérification IA

0 référence `app.ai.*`. Tables IA inchangées (12459/12459/3610/15/568/9). Aucun signal, aucun changement Kill Switch/readiness.

## 10. Limitations

Emails de test non confirmés par l'humain · `ADMIN_EMAILS` production non consulté (prudence) · isolation A/B non planifiable avec un seul compte proposé · aucun test réel effectué (aucun compte n'existe encore).

## 11. Verdict Final

**`PROVISIONING_PLAN_READY`**

Tous les prérequis techniques sont vérifiés fonctionnels. Les procédures sont documentées sans ambiguïté. Sécurité reconfirmée PASS. Plan de nettoyage honnête. Plan Phase 14.11 prêt.

---

## Fin de Phase

```
ADMIN_PROVISIONING:            READY (plan)
PROMOTER_PROVISIONING:         READY (plan)
ADMIN_EMAILS_CONFIGURATION:    READY (extension, jamais remplacement)
PROMOTER_CREATION_MECHANISM:   READY (POST /admin/promoters, admin requis)
SECURITY:                      PASS
CLEANUP_PLAN:                  READY (désactivation réversible uniquement)
PHASE_14_11_PLAN:               READY

DATABASE_WRITE:   NO
PAYMENT:          NO
CHARIOW:          NO
AI_MODIFIED:      NO
GIT_COMMIT:       NO
GIT_PUSH:         NO

VERDICT: PROVISIONING_PLAN_READY

HUMAN ACTION REQUIRED:
1. Confirmer ou fournir les 2 adresses email de test (Admin + Promoteur).
2. S'inscrire avec ces 2 adresses sur https://www.xfoot.site/login.html.
3. Sur Railway, ÉTENDRE (jamais remplacer) ADMIN_EMAILS avec l'email
   de test Admin.
4. Se connecter en Admin, créer le Promoteur via admin-promoters.html
   avec le user_id du compte de test Promoteur.
5. Revenir vers l'agent avec confirmation — la Phase 14.11 pourra
   alors être exécutée.
```

**STOP HERE. AUCUN COMPTE CRÉÉ. AUCUNE MODIFICATION DE ADMIN_EMAILS. AUCUN PROMOTEUR CRÉÉ.**

---

**PHASE 14.10R.1 — XFOOT ASSISTED TEST ACCOUNT PROVISIONING TERMINÉE. TOUS LES PRÉREQUIS TECHNIQUES SONT VÉRIFIÉS EN LECTURE SEULE, LES PROCÉDURES ADMIN ET PROMOTEUR SONT DOCUMENTÉES PAS À PAS, LA SÉCURITÉ EST RECONFIRMÉE PASS, ET LE PLAN DE LA PHASE 14.11 EST PRÊT. AUCUN COMPTE CRÉÉ. AUCUNE MODIFICATION DE ADMIN_EMAILS. AUCUN PROMOTEUR CRÉÉ. AUCUNE ÉCRITURE PRODUCTION. AUCUN PAIEMENT. AUCUNE MODIFICATION DU MOTEUR IA. STOP AVANT ÉCRITURE, COMME DEMANDÉ.**
