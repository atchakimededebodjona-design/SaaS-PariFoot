fastapi
uvicorn[standard]
numpy
scipy
# pandas/scikit-learn/xgboost/lightgbm : nécessaires pour SERVIR réellement
# une prédiction XGBoost/LightGBM depuis le service web (POST /models/
# ensemble/predict, et la passe shadow de generate_live_predictions -> voir
# app/ai/arena/models_common.py::XGBoostPredictionModel/LightGBMPredictionModel,
# imports paresseux à l'intérieur des fonctions de prédiction/disponibilité,
# jamais au niveau module — voir app/ai/arena/retraining.py pour le contre-
# exemple qui a causé un crash au démarrage, corrigé). Sans ces paquets,
# ces deux modèles restent honnêtement "indisponibles" (jamais un crash,
# voir check_availability) plutôt que de réellement prédire.
pandas
scikit-learn
xgboost
lightgbm
sqlmodel
python-jose[cryptography]
passlib[bcrypt]
# Épinglé : bug de compatibilité connu entre passlib (dernière release 1.7.4,
# projet non maintenu depuis 2020) et bcrypt>=4.1 — passlib sonde
# bcrypt.__about__.__version__, retiré dans bcrypt 4.1+, ce qui déclenche une
# erreur trompeuse "password cannot be longer than 72 bytes" dès le premier
# hash, sans rapport avec la longueur réelle du mot de passe.
bcrypt==4.0.1
python-multipart
httpx
psycopg2-binary
alembic
slowapi
email-validator
