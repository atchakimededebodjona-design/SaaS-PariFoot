# Dépendances du Cron Job Railway de ré-entraînement (refresh_and_retrain.py
# et sa chaîne d'imports : update_raw_data.py, export_model_artifacts.py,
# validate_artifacts.py). Distinct de api/requirements.txt (dépendances du
# service web) — le service web n'a pas besoin de pandas/scipy.optimize, et
# ce job n'a pas besoin de fastapi/uvicorn.
#
# Root Directory de ce service sur Railway = racine du dépôt (PAS api/,
# contrairement au service web) — voir RAILWAY_CRON_SETUP.md.
#
# Non épinglées, par cohérence avec api/requirements.txt (seul bcrypt y est
# épinglé, pour une raison de compatibilité documentée là-bas) — signalé
# comme risque de reproductibilité dans l'audit Phase 1, pas résolu ici.

pandas
numpy
scipy
sqlmodel
psycopg2-binary
httpx
